To download the libraries needed for the app, use the command:

$> expo install

To use the develop mod of the app, use the command:

$> expo start

To publish the app in public access, use the command:

$> expo publish
