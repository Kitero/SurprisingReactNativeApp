import { StyleSheet } from 'react-native';

const boxContainer = StyleSheet.create({
  boxSimple: {
    backgroundColor: '#fff',
    borderRadius: 5,
    borderWidth: 0.5,
    borderColor: '#000',
    padding: 20,
    justifyContent: 'space-around',
    margin: 'auto',
    marginTop: 100,
  },
});

export {
  boxContainer,
};
